service : {
  downloadFile: (nat) -> (opt blob);
  getUserFiles: () -> (vec record {
                             nat;
                             blob;
                           });
  uploadFile: (nat, blob) -> (text);
}
