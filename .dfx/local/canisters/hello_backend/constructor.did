service : {
  downloadFile: (nat) -> (opt record {
                                text;
                                blob;
                              });
  getUserFiles: () -> (vec record {
                             nat;
                             text;
                             blob;
                           });
  uploadFile: (nat, text, blob) -> (text);
}
